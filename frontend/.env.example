# For local dev, leave this unset (api.ts falls back to the Vite proxy).
# For production (e.g. on Vercel), set this to your deployed backend's URL:
VITE_API_URL=https://your-backend-name.onrender.com/api
